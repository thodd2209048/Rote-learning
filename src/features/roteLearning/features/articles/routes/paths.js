const articles = "/articles";
const repetition = articles + "/repetition";
const addArticle = articles + "/add";
const updateArticleWithId = articles + "/article-update/:id";

export const paths = { articles, repetition, addArticle, updateArticleWithId };
